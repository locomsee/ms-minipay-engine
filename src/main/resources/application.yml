spring:
  application:
    name: ms-minipay-engine
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  jackson:
    default-property-inclusion: non_null
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: ${PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

minipay:
  gateway:
    mode: deterministic # deterministic | random
    threshold: 10000
    random-success-rate: 0.8
  notification:
    executor:
      core-pool-size: 2
      max-pool-size: 5
      queue-capacity: 100
  jwt:
    secret: ${JWT_SECRET:change-this-dev-only-secret-key-minimum-256-bits-long}
    expiration-ms: 3600000 # 1 hour

resilience4j:
  retry:
    instances:
      smsProvider:
        max-attempts: 3
        wait-duration: 200ms
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2
  circuitbreaker:
    instances:
      smsProvider:
        sliding-window-size: 10
        minimum-number-of-calls: 5
        failure-rate-threshold: 50
        slow-call-duration-threshold: 2s
        slow-call-rate-threshold: 50
        wait-duration-in-open-state: 10s
        permitted-number-of-calls-in-half-open-state: 3
  ratelimiter:
    # "configs" (not "instances"): reusable templates selected dynamically per request by
    # RateLimitingFilter, one independent limiter per client key (IP or username) sharing
    # the named template below. timeout-duration: 0s makes acquirePermission() fail fast
    # instead of blocking a Tomcat thread waiting for a permit.
    configs:
      login:
        limit-for-period: 5
        limit-refresh-period: 1m
        timeout-duration: 0s
      webhook:
        limit-for-period: 20
        limit-refresh-period: 1m
        timeout-duration: 0s
      paymentInitiation:
        limit-for-period: 10
        limit-refresh-period: 1m
        timeout-duration: 0s
